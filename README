Instalación:
   git clone git://github.com/alucardni/puntovim.git ~/.vim

Crear enlaces simbólicos:
   ln -s ~/.vim/vimrc ~/.vimrc

Cambiar al directorio '~/.vim' y obtener los submódulos:

   cd ~/.vim
   git submodule init
   git submodule update
